# Evidence — DE Sales Order-Level Port (2026) and 2025 DE Rebuild

**Date:** 2026-07-30
**Files:** `reports/digital-marketing-member-pages/api/salesde25.js` (renamed from `salesde.js`), `pages/salesde25.html` (renamed from `salesde.html`), `pages/sales.html`, `api/assign-order.js`

## Purpose
1. Port the DE (ledsone.de) dashboard to the same order-level, zero-duplication architecture already proven on `salesuk.js`/`sales25.js`, using the real ownership rules (Jeffri/Thasitha/Mahima/Sukirtha) already live on the product-scoped `api/sales.js`.
2. Extend to Feb–Jul 2026, then — per user follow-up — repurpose the entire file/page pair for 2025 DE data only (2026 DE is already covered by the per-person tabs on the main dashboard), scoped to Jan–Jun 2025, Jeffri/Mahima/Sukirtha only (Thasitha dropped).
3. Split Mahima's tab into Ads and Organic sub-tabs on the 2025 DE page specifically.

## What Was Done

### 2026 DE port (superseded, see below)
- Ported `MAHIMA_EXCLUDED_PRODUCT_IDS` (~1,000+ product IDs) verbatim from `api/sales.js`, plus matcher functions for Jeffri Ads/Meta, Thasitha Ads, Mahima Ads+product, Sukirtha catch-all — expressed as order-level `GROUPS` (first-match-wins).
- Extended the Shopify GraphQL query to fetch `variant { product { legacyResourceId } }` per line item (needed for Mahima's product-scoped ownership check).
- Added Feb–Jul 2026 month tabs. Verified zero duplicates, zero missed orders (aside from cancelled/test exclusions) across all 6 months via `meta.ordersFetched` cross-check.

### Repurposed for 2025 (per user: "no need standalone for 26 already 26 have by person tab in sales.html lets create for 25 only")
- `git mv api/salesde.js api/salesde25.js`, `git mv pages/salesde.html pages/salesde25.html` (preserves history).
- Changed `SUPPORTED_MONTHS`/`CURRENT_LIVE_MONTHS` to Jan–Jun 2025 (checked live order counts existed for all 12 months of 2025 before scoping down to 6 per user follow-up).
- Renamed the assign-override `source` key from `'salesde'` to `'salesde25'` in both files and `assign-order.js`'s `VALID_SOURCES`.
- Renamed the static-snapshot filename prefix to `salesde25-*` for future clarity (no snapshots existed yet at time of rename — always-live queries).
- Removed the Thasitha group entirely from `GROUPS` (per user: "leave thasitha... do for jeffri, mahima, sukirtha").
- Renamed the `sales.html` nav tab from "DE Sales" → "2025 DE" and repointed it to `salesde25.html` (user's literal instruction said "2025 UK" but the naming symmetry with "UK Sales"/"2025 UK" made "2025 DE" the clear intent — flagged to user, no correction received).

### Mahima Ads/Organic split (2025 DE only)
- Replaced the single `mahima` group with `mahima-ads` (ad-campaign-matched only) and `mahima-organic` (not ad-matched, but contains an owned product — including no-journey-data orders).
- Ad-click wins on overlap, per explicit user confirmation ("if that pur organic only add to organic").
- Added both new keys to `assign-order.js`'s `VALID_GROUP_KEYS`.

## Verification (live curl, post-deploy)

2026 DE, all 6 months, 0 duplicates/missed (example):
```
Jul 2026: jeffri 213, thasitha 19, mahima 197, sukirtha 65, not-assigned 24, total 518 = raw 518
```

2025 DE (post-scope-down to Jan-Jun, Thasitha dropped):
```
Jan 2025 { jeffri: 81, mahima: 271, sukirtha: 58, not-assigned: 182 } sum:592 unique:592 raw:592 dupes:0
Jun 2025 { jeffri: 114, mahima: 188, sukirtha: 25, not-assigned: 105 } sum:432 unique:432 raw:433 dupes:0 (1 cancelled/test order)
```

Mahima Ads/Organic split, January 2025:
```
jeffri: 81, mahima-ads: 0, mahima-organic: 271, sukirtha: 58, not-assigned: 182
unique: 592 | raw: 592 | dupes: 0
```
(Mahima Ads showing 0 is a real finding — no DE orders this month carry her ad-campaign signal — not a bug; confirmed by re-checking the combined pre-split total matched exactly.)

## Status
Live and verified. Pushed to both `staff/main` (via `staff-sync29` worktree) and `aios-2`.

## PASS/FAIL
PASS — zero duplication by construction (priority-ordered GROUPS array) and confirmed live at every stage.

## Next Step
Awaiting user input on whether a DE-specific Mahima ad-campaign naming pattern exists that the current (UK-ported) `isMahimaAdsMatch` rule is missing.
