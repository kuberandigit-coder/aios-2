# Daily Log — 2026-06-17

Each task below has its own detailed file in this folder.

## Tasks
1. **[Ledsone.de — Core Web Vitals / LCP investigation](LEDSONE_DE_LCP_PERFORMANCE_AUDIT.md)**
   Full file-level root-cause analysis of why LCP fails (3.6/3.8s). Identified the hero double-download, oversized image, ~330KB render-blocking CSS, wasteful font preloads, lazy above-fold logo, and duplicated Google Fonts requests — each traced to exact files/lines. Logged an honesty correction on the font-preload claim.

2. **[Ledsone.de — theme optimization changes](LEDSONE_DE_THEME_OPTIMIZATION_CHANGES.md)**
   Prepared 11 exact code edits (P0–P3). Applied the safe set; built an updated theme folder + a valid Shopify zip. Change #3 (defer base.css) broke rendering on the live theme and was reverted. Documented the safe critical-CSS path, remaining method-only items, and the Shopify CLI workflow for future direct edits. Nothing pushed to the live store.

3. **[Shopify→Sheets UTM / Product report extension](LEDSONE_UTM_PRODUCT_REPORT_EXTENSION.md)** _(Apps Script)_
   Extended the existing UTM Apps Script with a Product×UTM report (May+June, filtered by Product IDs, 4 tabs) reusing existing attribution logic with one additive GraphQL field. Delivered production-ready module + validation. Open issue: `Invalid timestamp for created_at` (0 orders) — provided RFC3339 fix + a diagnostic function; leading hypothesis is the store clock predates the hardcoded 2026 dates. Pending diagnostic output.
