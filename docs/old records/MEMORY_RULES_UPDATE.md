# Memory & Working-Rules Update

**Date:** 2026-06-12

## What changed
Synced and expanded Kuberan's working rules, using `website technical - Kuberan\claude-memory\` as the single source of truth (disconnected from the old `.claude` memory folder).

## Rules confirmed / added
- **No git push without explicit permission** (confirmed)
- **All .md → dated folder** `website technical - Kuberan\YYYY-MM-DD\` (confirmed)
- **"done for today"** → daily log + push dated folder to aios-kuberan only, `requirements/YYYY-MM-DD/` (confirmed)
- **"push blog tool"** → index.html → blog-builder origin/master (confirmed)
- **loyalty-dashboard-de** → Shopify loyalty app, deploy on Render + Neon (ADDED)
- **Daily log: no duplicate tasks** — each item logged once (ADDED)
- **Per-task md files** — on "done for today", write a separate named .md per task/change, plus the daily log that links them (ADDED)

## Source of truth
`C:\Users\PC\OneDrive\Desktop\website technical - Kuberan\claude-memory\`
All rules read/written here; old `.claude` memory folder no longer used.
