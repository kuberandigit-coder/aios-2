# Validation — DE Sales Order-Level Port (2026) and 2025 DE Rebuild

**Date:** 2026-07-30

## Checks Performed
1. `node --check` on `api/salesde25.js` and `api/assign-order.js` after every edit — all passed.
2. Embedded `<script>` block in `pages/salesde25.html` parsed via `new Function()` — passed, at every stage of the rebuild (rename, month-scope-down, Thasitha removal, Mahima split).
3. Live curl verification of all 5 groups (`jeffri`, `thasitha`, `mahima`, `sukirtha`, `not-assigned`) for all 6 months of 2026 DE (Jan-Jul): cross-referenced `orderLegacyId` sets, confirmed 0 duplicates, totals matched `meta.ordersFetched` raw truth in every month.
4. Live curl check of all 12 months of 2025 DE order data (`meta.ordersFetched`) before deciding which months to build tabs for — all 12 had real orders (382-803/month) before user scoped down to Jan-Jun only.
5. Live curl verification of `jeffri`/`mahima`/`sukirtha`/`not-assigned` for Jan-Jun 2025 post-Thasitha-removal: 0 duplicates, totals matched raw truth (1 cancelled/test exclusion in June, expected).
6. Live curl verification of `mahima-ads`/`mahima-organic` split for January 2025: 0 overlap, combined total (0 + 271 = 271) matched the pre-split combined Mahima total exactly.
7. Confirmed final page routing and nav: `sales.html`'s "2025 DE" tab points to `salesde25.html`; old `salesde.html` URL correctly gone (renamed, not duplicated).

## Result
PASS on all checks. No duplication, no unexpected missed orders, correct final routing and nav labeling.
